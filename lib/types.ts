export type Category = {
    id: string;
    title: string;
    description:string;
}

export type Post = {
    id: string;
    userId: string;
    title: string;
    content: string;
    image:string;
}