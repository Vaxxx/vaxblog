import React from 'react';
import UserCard from "@/app/ui/UserCard";

const RegisterPage = () => {
    return (
        <div>
            <UserCard/>
        </div>
    );
};

export default RegisterPage;